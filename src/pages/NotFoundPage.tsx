import { BackgroundImage, Container } from "../ui";

export const NotFoundPage = () => {
  return (
    <main>
      <BackgroundImage />
      <Container>
        <h1>NotFound</h1>
      </Container>
    </main>
  );
};
